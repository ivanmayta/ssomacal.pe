---
import Container from "../components/Container.astro"
import SERVICIOS from "../data/services"
---

<section class="py-20">
    <Container>
        <h2 class="text-3xl font-extrabold text-center mb-6 text-[#0b1f74]">
            ¿Qué ofrecemos?
        </h2>
        <hr class="w-14 self-center border-2 border-[#0b1f74]" />
        <p class="text-center mt-6 max-w-5xl mx-auto font-bold text-lg">
            Diseño, Desarrollo e Implementación de Sistema de Gestión de
            Seguridad y Salud en el Trabajo. Norma ISO 45001. Matriz IPERC.
            Reglamento Interno de SST. Comité o Supervisor de SST. Protocolos de
            Trabajo Seguro. Plan de Emergencia y Plan de Contingencia.
        </p>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 mt-14">
            {
                SERVICIOS.map((service) => (
                    <a
                        href={`/servicios/${service.id}`}
                        class="flex flex-col bg-white shadow-md p-6 rounded-lg text-center items-center"
                    >
                        <img
                            class="w-full lg:aspect-auto aspect-square object-cover transition-transform hover:animate-pulsing infinite"
                            src={service?.images[0]}
                            alt=""
                        />
                        <h3 class="text-2xl font-bold mt-4 mb-2 text-[#0b1f74]">
                            {service.title}
                        </h3>
                        <p class="text-lg font-semibold opacity-90">
                            {service.description}
                        </p>
                    </a>
                ))
            }
        </div>
    </Container>
</section>
<style>
    .infinite {
        animation-iteration-count: infinite;
    }
</style>
